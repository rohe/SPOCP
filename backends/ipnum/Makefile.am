if BUILD_IPNUM
lib_LTLIBRARIES = libipnumplugin.la

libipnumplugin_la_SOURCES = ipnum.c
libipnumplugin_la_LDFLAGS = -module
libipnumplugin_la_LIBADD = ../../lib/libspocp.la

INCLUDES = -I$(srcdir)/../../include
endif BUILD_IPNUM

